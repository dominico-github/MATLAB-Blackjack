% Program that generates a random integer value between 1 and 11 to mimic 
% the playing card values for the blackjack game

% Generates a random integer between 1 and 11
n = randi(11);

disp(n);